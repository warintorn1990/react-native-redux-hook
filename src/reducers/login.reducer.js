const initialState = {
    username: "arm",
    password: "1234567890"
}

export default (state = initialState, { type, payload }) => {
    switch (type) {  

    default:
        return state
    }
}
